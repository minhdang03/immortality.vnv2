{
  "name": "@btd/notion",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --local",
    "deploy": "wrangler deploy",
    "deploy:staging": "wrangler deploy --env staging",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:ai-prompts": "vitest run test/ai-refuse-patterns.test.ts"
  },
  "dependencies": {
    "hono": "^4.6.3",
    "@anthropic-ai/sdk": "^0.27.3",
    "@notionhq/client": "^2.2.15"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.5.37",
    "@cloudflare/workers-types": "^4.20241218.0",
    "typescript": "^5.6.3",
    "vitest": "^2.1.8",
    "wrangler": "^3.91.0"
  }
}
