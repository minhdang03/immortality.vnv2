// @btd/firebase-config — shared Firebase app initialisation for web + mobile
// Full extraction from apps/web/src/firebase.js planned for Phase 5.
// For now apps/web consumes its own firebase.js directly.

export {};
